#include<iostream>
#include<string>

// using namespace std; - Remove this line for omitting namespace
/*omitted and replaced with the std keyword,
followed by the :: operator for string (and cout) objects:*/

int main(){

    std::string name = "Error";
    std::cout << name;
    return 0;
}
